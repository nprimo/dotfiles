[env]
TERM = "xterm-256color"

[shell]
program = "/usr/bin/fish"
args = ["-l", "-c", "tmux attach || tmux"]

[window]
decorations = "None"

[keyboard]
bindings = [{ key = "F11", action = "ToggleFullscreen" }]
