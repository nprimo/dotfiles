[env]
TERM = "xterm-256color"

[shell]
program = "/usr/bin/zsh"
args = ["-l", "-c", "tmux attach || tmux"]

[window]
padding.x = 16
padding.y = 14
decorations = "None"
opacity = 0.98

[keyboard]
bindings = [{ key = "F11", action = "ToggleFullscreen" }]
